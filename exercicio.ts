let mutiplica = (multiplicando: number, multiplicador: number) : number => multiplicando * multiplicador

console.log(mutiplica(10,5))

let saldação = (nome:string): string => `Olá ${nome}`
console.log(saldação('Bruno'))